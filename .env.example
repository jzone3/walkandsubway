# Optional: real pedestrian routing for walking legs via openrouteservice.
# Without a key the app falls back to straight-line estimates (no behavior change).
# Free key: https://account.heigit.org (verify your plan's daily quota on the dashboard).
ORS_API_KEY=
ORS_BASE_URL=https://api.openrouteservice.org
